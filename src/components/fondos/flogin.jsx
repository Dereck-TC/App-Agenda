import styled from "styled-components";

const flogin = styled.div`
    background: var(--background-gradienta);
    background-blend-mode: var(--background-blend-mode);    
    height: 100%;
`

export default flogin;